# Workflow

```text
update local platform clone
        |
        v
run local platform MCP
        |
        v
query or validate platform truth from component repo
        |
        v
write local OpenSpec artifacts in the component repo
```
