# Local Read-Only Rules

- treat the platform repo as canonical truth
- treat the component repo as local OpenSpec truth
- keep the MCP server read-only in v1
- default to the component's pinned platform version
- allow latest-version queries only for exploration, not default validation
- do not let JIRA become the detailed spec source
- do not copy editable platform truth into component repos
