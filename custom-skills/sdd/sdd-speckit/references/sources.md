# Sources

This skill package was synthesized from:
- Spec Kit homepage and usage guide
- Spec Kit README
- Spec Kit docs for installation, quick start, and upgrade

Key themes captured here:
- specifications become executable artifacts
- the workflow centers on constitution -> specify -> clarify -> plan -> tasks -> analyze -> implement
- initialization uses the `specify` CLI with agent-specific integration
- upgrades refresh command/template/script files while leaving `specs/` safe
