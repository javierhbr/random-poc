# Sr. Fullstack Developer Workspace

Copy this folder to:

```text
~/.openclaw/workspaces/sr-fullstack-developer
```

Then create the agent state directory:

```bash
mkdir -p ~/.openclaw/agents/sr-fullstack-developer
```

Recommended convenience link:

```bash
mkdir -p ~/.openclaw/workspaces/sr-fullstack-developer/links
ln -s ~/coding-projects ~/.openclaw/workspaces/sr-fullstack-developer/links/coding-projects
```
