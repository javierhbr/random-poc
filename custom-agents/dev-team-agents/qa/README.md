# QA Engineer Workspace

Copy this folder to:

```text
~/.openclaw/workspaces/qa-engineer
```

Then create the agent state directory:

```bash
mkdir -p ~/.openclaw/agents/qa-engineer
```

Recommended convenience link:

```bash
mkdir -p ~/.openclaw/workspaces/qa-engineer/links
ln -s ~/coding-projects ~/.openclaw/workspaces/qa-engineer/links/coding-projects
```
